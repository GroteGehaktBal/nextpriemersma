# Copy to .env.local and fill in. None of these are required to run the site.

# Password for routes enabled in `protectedRoutes` (src/app/resources/config.js).
# Leave unset to keep password protection switched off.
SITE_PASSWORD=

# Set to 1 to print Next.js telemetry debug output.
NEXT_TELEMETRY_DEBUG=
